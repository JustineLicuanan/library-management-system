{
	"private": true,
	"scripts": {
		"commit": "git add . && git commit -m",
		"push": "git push origin master",
		"heroku": "git push heroku master",
		"cwd:server": "yarn --cwd ./packages/server",
		"cwd:web": "yarn --cwd ./packages/web",
		"i": "yarn cwd:server && yarn cwd:web"
	}
}
